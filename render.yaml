services:
  - type: worker
    name: aksiya_halol_bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python pro_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
