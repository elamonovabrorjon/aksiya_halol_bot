services:
    - type: worker
    name: aksiya-halol-bot
    env: python
    runtime: python3.11.0
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
            - key: TELEGRAM_BOT_TOKEN
        sync: false
            - key: ADMIN_IDS
        value: 745170275