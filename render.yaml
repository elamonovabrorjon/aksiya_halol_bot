services:
    - type: worker
    name: aksiya-halol-bot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
            - key: TOKEN
        sync: false